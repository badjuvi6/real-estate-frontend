# real-estate-app/backend/.env

# Port for your backend server.
# The frontend's main.js is configured to use http://localhost:3000 by default.
PORT=3000

# MongoDB connection URI.
# Choose ONE of the options below:

# Option A: For local MongoDB (if you installed MongoDB Community Server on your machine)
# Make sure your 'mongod' process is running (how to start depends on your OS setup).
MONGODB_URI=mongodb://localhost:27017/realestate_db

# Option B: For MongoDB Atlas (cloud database) - uncomment and replace with your details.
# This is recommended for production or if you prefer a cloud database.
# MONGODB_URI=mongodb+srv://<your_username>:<your_password>@<your_cluster_name>.mongodb.net/realestate_db?retryWrites=true&w=majority

# IMPORTANT:
# If using MongoDB Atlas:
# 1. Replace <your_username>, <your_password>, and <your_cluster_name> with your actual MongoDB Atlas credentials.
# 2. In your MongoDB Atlas dashboard, go to 'Network Access' and add your current public IP address
#    (or 0.0.0.0/0 for testing purposes, but avoid this in production for security).
# 3. Ensure you have created a database user with password access.# real-estate-app/backend/.env

# ... (your existing PORT and MONGODB_URI) ...

# Cloudinary Credentials
CLOUDINARY_CLOUD_NAME=dztkg2o3f
CLOUDINARY_API_KEY=382862493954335
CLOUDINARY_API_SECRET=sYJXs7iyyLhOtRd6VdnBux06V-4